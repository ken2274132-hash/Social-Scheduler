node_modules
.next
.DS_Store
*.log
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.next-prod

# Local database dumps (see docs/BACKUPS.md) -- never commit these
backups/

# Cloudflare / OpenNext build output
.open-next/
.wrangler/
cloudflare-env.d.ts

# Wrangler local secrets — never commit
.dev.vars
.dev.vars.*

# Local Claude Code settings — machine-specific, deliberately untracked.
.claude/
